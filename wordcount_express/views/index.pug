extends layout.pug

block content
  h1= title
  p.header Welcome to #{title}
  p
    button.count(onclick="clickme()") Count!
  p
    div(id="countResults" class="results")
        ul(id="topTen")
  p

